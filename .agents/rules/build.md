# Build Command Restrictions

- NEVER run `npm run build` or `next build` without explicit permission from the user.
- Always rely on `npm run dev` and TypeScript / lint diagnostics instead for testing during development.
